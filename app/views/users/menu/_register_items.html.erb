<% if user_signed_in? %>
    <%= link_to('Modifier le compte', edit_user_registration_path, :class => "btn btn-primary") %>
<% else %>
    <%= link_to("S'inscrire", new_user_registration_path, :class => "btn btn-primary")  %>
<% end %>
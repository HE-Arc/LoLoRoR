<div class="well">
<% if user_signed_in? %>
  <% if @dashboards_light.count > 0 %>
    <h4>Mes Dashboards</h4>
    <ul class="list-group">
      <% @dashboards_light.each do |dash| %>
      <li class="list-group-item"><%= link_to(dash.name, dash) %></li>
      <% end %>
    </ul>
  <% else  %>
  Créez votre premier dashboard depuis <%= link_to('l\'interface de gestion de dashboards', users_dashboards_path) %>
  <% end %>
    <h4>En partie</h4>
  <% if @playingUsers.count > 0 %>
    <ul class="list-group">
      <% @playingUsers.each do |user| %>
      <li class="list-group-item">
        <%= link_to user.pseudoLoL, account_path(:region => user.region, :idLoL => user.idLoL) %>
      </li>
      <% end %>
    </ul>
  <% else %>
    Aucun ami en partie
  <% end %>

<% else %>

   <p>LoLTracker vous permet de créer vos propres dashboards et de consulter les joueurs en cours de partie !</p>
   <p><%= link_to "Inscrivez-vous", new_user_registration_path %> ou <%= link_to "connectez-vous", new_user_session_path %> pour accéder à toutes les fonctionnalités !</p>


<% end %>
</div>
<%= form_for @account, :url => url_for(:controller => 'accounts_users', :action => 'create'), html: {method: "post", class: "inline-form"}  do |f| %>
<%= f.hidden_field :id, :value => @account.id %>
<%= f.submit "Ajouter à mes comptes", class: 'btn btn-success btn-xs' %>

<% end %>
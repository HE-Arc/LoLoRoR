<%= form_for @account, :url => url_for(:controller => 'accounts_users', :action => 'create'), html: {method: "post"}  do |f| %>

<p>
  <%= f.hidden_field :id, :value => @account.id %>
  <%= f.submit "Add to your accounts", class: 'btn btn-success' %>
</p>

<% end %>
<%= form_for @account, :url => url_for(:controller => 'accounts_trackgroups', :action => 'create'), html: {method: "post"}  do |f| %>

<p>
  <%= f.label :trackgroups %>
  
  <%= f.collection_select(:trackgroups, @trackgroups, :id, :name) %>
</p>

<p>
  <%= f.hidden_field :id, :value => @account.id %>
  <%= f.submit "Add to this trackgroup", class: 'btn btn-success' %>
</p>

<% end %>